# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
name = Blanc
email = 1mayanktomar@gmail.com

[merge]
    conflictStyle = "diff3"
    tool = "nvimdiff2"

[merge "mergiraf"]
    name = mergiraf
    driver = mergiraf merge --git %O %A %B -s %S -x %X -y %Y -p %P -l %L

[core]
    attributesfile = ~/.gitattributes
